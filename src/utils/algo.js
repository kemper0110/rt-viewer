
// Ищет заявки, в которых не было изменений статуса более N дней
function getUnchangedRequests(data, N) {
    const ids_last_req = new Map();
    for(const row of data) {
        const id = row['Номер заявки']

    }
}